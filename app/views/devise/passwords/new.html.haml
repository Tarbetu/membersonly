%h2.title.has-text-centered.m-2 Forgot your password?
.block.m-5
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field
      .control= f.label :email, class: "label"
      .control= f.email_field :email, autofocus: true, autocomplete: "email", class: "input"
    .actions
      .control= f.submit "Send me reset password instructions", class: "button"
  = render "devise/shared/links"
